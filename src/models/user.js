export default class User {
  constructor(username, password, email, name, lastname, rol) {
    this.username = username;
    this.password = password;
    this.email = email;
    this.name = name;
    this.lastname = lastname;
    this.rol = rol;
  }
}
